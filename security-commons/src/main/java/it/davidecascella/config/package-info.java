/**
 * Spring Security configuration components for OAuth2/JWT and CORS.
 * <p>
 * This package provides shared security configuration for Spring Boot applications
 * using AWS Cognito as identity provider, including JWT validation, authority
 * conversion from Cognito groups, and CORS setup.
 */
package it.davidecascella.config;
